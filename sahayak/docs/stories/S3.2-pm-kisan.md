# S3.2: PM-KISAN Status Lookup

**Epic:** E3 — Government Data MCP Server
**Priority:** P0 | **Points:** 5 | **Sprint:** W2
**Status:** Complete

## Acceptance Criteria

get_pm_kisan_status tool returns beneficiary data for state/district/block. Shows last installment info. Handles: no data found, partial match, API errors. Returns clear 'no data available' message, not error.

## Tasks & Verification

### T3.2.1 — get_pm_kisan_status tool
- **DoD:** Returns beneficiary data for state/district. 3 states tested. No data → clear message. Partial input (state only) → state-level summary.
- **Status:** ✅ Complete
- **Evidence:**
  - `pmkisan.py:18-67` — accepts `state`, `district`, optional `block`. Title-case normalization on all filters.
  - No data: returns `PMKisanResponse(message="No PM-KISAN data found for {district}, {state}")` — clear message, not error.
  - API error: returns `PMKisanResponse(message="Error fetching PM-KISAN data: {exc}")` — structured response, not exception.
  - Retry: uses shared `fetch_with_retry()` from `cache.py` (1 retry on 5xx).
  - Cache: 12-hour TTL (`_PMKISAN_TTL = 12 * 3600`).
  - Response model: `PMKisanStatus(state, district, beneficiary_count, last_installment, amount_per_installment)`.
  - Resource ID: `a2dac80e-8e2c-4d0e-8194-5b498c9e24f3`.
- **Fix applied:** Added retry-on-5xx via shared `fetch_with_retry`.

## Key Files

| File | Purpose |
|------|---------|
| `govdata_india/server.py:35-49` | pm_kisan_status MCP tool |
| `govdata_india/pmkisan.py` | API client with caching + retry |
| `govdata_india/types.py:25-38` | PMKisanStatus, PMKisanResponse models |
| `tests/test_server.py:95-131` | PM-KISAN tests (success + no data) |
